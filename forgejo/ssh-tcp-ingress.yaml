apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  generation: 1
  name: forgejo-ssh
  namespace: forgejo
spec:
  entryPoints:
  - ssh
  routes:
  - match: HostSNI(`*`)
    services:
    - name: forgejo
      port: 22
